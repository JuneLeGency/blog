# blog

个人博客，基于 Jekyll + GitHub Pages。

线上地址：<https://JuneLeGency.github.io/blog/>

## 写新文章

在 `_posts/` 下新建 `YYYY-MM-DD-title.md`，顶部加 front matter，正文写 Markdown，`git push` 即可。

## 本地预览

```bash
bundle install
bundle exec jekyll serve
```

访问 <http://localhost:4000/blog/>。
